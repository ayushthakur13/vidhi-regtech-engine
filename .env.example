# Local dev — use a local Postgres connection string while developing
DATABASE_URL=postgresql://vidhi:vidhi_local_dev@localhost:5432/vidhi
REDIS_URL=

# Get one at https://console.groq.com
GROQ_API_KEY=
GROQ_API_KEY_ALTERNATIVE=

# Model used for grounded obligation extraction
GROQ_MODEL=

# Embedding dim must match db/schema.sql (default 384 for all-MiniLM-L6-v2)
EMBEDDING_MODEL=
